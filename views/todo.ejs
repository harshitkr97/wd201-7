<table>
    <% for (let index = 0; index < allTodos.length; index++) { %>
        <tr>
            <td>
                <%= allTodos[index].title %>
            </td>
        </tr>
    <% } %>
</table>
